# shared-utils-helpers

> This library was generated with [Nx](https://nx.dev).

Shared helper utils for the project.

## Building

Run `nx build shared-utils-helpers` to build the library.

## Running unit tests

Run `nx test shared-utils-helpers` to execute the unit tests via [Vitest](https://vitest.dev/).

## Running linter

Run `nx lint shared-utils-helpers` to execute the lint via [ESLint](https://eslint.org/).
