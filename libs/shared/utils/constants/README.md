# shared-utils-constants

> This library was generated with [Nx](https://nx.dev).

Shared constants for the project.

## Building

Run `nx build shared-utils-constants` to build the library.

## Running linter

Run `nx lint shared-utils-constants` to execute the lint via [ESLint](https://eslint.org/).
