# signing-webapp-form-ui-form-group

> This library was generated with [Nx](https://nx.dev).

Wrapper component for form group. It provides a consistent way to display form groups in the signing webapp.

## Running unit tests

Run `nx test signing-webapp-form-ui-form-group` to execute the unit tests.

## Running linter

Run `nx lint signing-webapp-form-ui-form-group` to execute the lint via [ESLint](https://eslint.org/).
