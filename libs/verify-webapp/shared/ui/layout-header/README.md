# verify-webapp-shared-ui-layout-header

> This library was generated with [Nx](https://nx.dev).

The header component is a part of the shared UI layout for the web application. It provides a consistent header across different pages and sections of the application.

## Running unit tests

Run `nx test verify-webapp-shared-ui-layout-header` to execute the unit tests.

## Running linter

Run `nx lint verify-webapp-shared-ui-layout-header` to execute the lint via [ESLint](https://eslint.org/).
